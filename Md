# Forge AI SDK (Prototype)

Drop-in TypeScript SDK + minimal Express API for AutoGenesis. 
Implements:
- Account security helpers (Argon2 hash, AES-256 encryption, JWT, TOTP 2FA).
- Cost/pricing estimator.
- Blueprint JSON generator (12 parts + full vehicle).
- Marketing copy generator.
- Test drive telemetry simulator.

## Get started

```bash
npm install
npm run dev       # starts the API on :8080
